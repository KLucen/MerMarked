# 选区映射验收

这是普通文本，用于验证单块单文本节点。

重复词先出现，重复词再次出现，不能混淆。

这里有**加粗内容**与[链接文字](https://example.org/)。

中文 ABC，emoji 😀，组合字符 é 与全角Ａ、半角A。

转义字符 \*星号\*、实体 &amp;。

这里有 `行内代码`。

跨节点前半段 **跨节点后半段**。

跨段落前半段。

跨段落后半段。
